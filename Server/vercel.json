{
  "buildCommand": "cd backend && npm install && npx prisma generate && cd ../Front-cabinet && npm install && npm run build",
  "outputDirectory": "Front-cabinet/dist",
  "functions": {
    "backend/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}